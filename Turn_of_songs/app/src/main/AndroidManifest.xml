<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.turn_of_songs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Turn_of_songs">
        <activity android:name=".Menu.Start.SignupActivity"></activity>
        <activity android:name=".Menu.Start.LoginActivity" />
        <activity android:name=".Menu.Start.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu.Profile.ThirdActivity" />
        <activity android:name=".Menu.Profile.SecondActivity" />
        <activity android:name=".Menu.Profile.MainActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>